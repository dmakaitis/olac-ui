{
  "name": "config",
  "version": "1.0.1",
  "description": "Config Utility",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.556.0"
  }
}
