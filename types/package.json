{
  "name": "@olac/types",
  "version": "1.0.0",
  "private": true,
  "description": "Common types for the OLAC website code",
  "types": "src/index.d.ts",
  "scripts": {
    "clean": "rimraf -g '*([src|test])/**/*.js' '*([src|test])/**/*.d.ts'",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}